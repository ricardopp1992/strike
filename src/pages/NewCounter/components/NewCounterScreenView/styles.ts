import { normalTheme } from "@/theme";
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    height: '100%',
    marginTop: '5%',
    paddingHorizontal: '3%',
    alignItems: 'center',
  },
  title: {
    fontSize: 20,
    fontWeight: '500',
    marginBottom: '5%'
  },
  startCountingButton: {
    position: 'absolute',
    bottom: '10%'
  }
})